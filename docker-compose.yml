# Generated by compile.py; edits are overwritten on the next build.
services:
  effect:
    build: ./runtime
    container_name: expert-effect
    restart: unless-stopped
    ports: ["8801:8801"]
    environment:
      EXPERT_NAME: effect
      EXPERT_GRAPH: /graph/graph.json
      EXPERT_REPO: /repo
      EXPERT_PORT: "8801"
    volumes:
      - ./experts/effect/repo:/repo:ro
      - ./experts/effect/graph:/graph:ro

  graphify:
    build: ./runtime
    container_name: expert-graphify
    restart: unless-stopped
    ports: ["8802:8802"]
    environment:
      EXPERT_NAME: graphify
      EXPERT_GRAPH: /graph/graph.json
      EXPERT_REPO: /repo
      EXPERT_PORT: "8802"
    volumes:
      - ./experts/graphify/repo:/repo:ro
      - ./experts/graphify/graph:/graph:ro
