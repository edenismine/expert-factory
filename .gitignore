# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Upstream clones and multi-MB graphs: reconstructible from expert.json's source
# list. raw/ and notes/ are NOT ignored — fetched content suffers link rot and
# notes are original work, so neither can be rebuilt.
experts/*/repos/
experts/*/graph/
